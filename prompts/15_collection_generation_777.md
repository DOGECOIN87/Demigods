# Exact 777-Token Generation Rules

```text
Using the completed modular asset library, produce exactly 777 unique final compositions.

- Token IDs range from 0001 through 0777.
- Every token uses the same 1254×1254 canvas.
- No two tokens may share the same complete ordered trait signature.
- Use deterministic generation with a recorded random seed.
- Maintain a uniqueness registry containing selected trait IDs for every token.
- Reject duplicate signatures before rendering.
- Apply compatibility exclusions only where visual collisions occur.
- Do not reduce the asset library merely to make its mathematical product equal 777.
- Stop immediately after 777 validated unique outputs.
- Export one metadata JSON record per image.
- Record every selected category and source filename.
- Produce a collection-level provenance hash after final ordering.
```
