# Master Extraction and Rebuilding Prompt

```text
Use the supplied reference sheet only as visual source material. Identify the specifically named target trait, reconstruct it cleanly, and output that single trait on its own standardized canvas.

Do not crop the trait directly from a flattened sheet when the source contains baked-in backgrounds, labels, body parts, neighboring assets, inconsistent lighting, or low-resolution edges. Rebuild and clean the design while preserving its recognizable silhouette, materials, colors, and details.

Use the locked 1254×1254 master rig, exact category anchors, front-facing orientation, and standardized upper-left lighting. Remove all unrelated layers. Preserve hidden overlap where needed to prevent seams when composited. Output a true-alpha PNG with no rendered checkerboard, no labels, no frame, and no character name.

Generate exactly one target asset per output. Do not generate a contact sheet, variant board, infographic, or completed character.
```
